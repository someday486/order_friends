# 2026-04-04 패치노트

## 개선 사항
- 08:12 · Order Friends Dev · 주문 추적 화면에서 주문 취소를 누르면 바로 처리하지 않고 한 번 더 확인하는 안내 모달을 표시하도록 바꿨습니다.
- 21:03 · Order Friends Dev · 주문 취소 확인 모달 안에서 주문 메뉴와 수량을 간단히 확인할 수 있게 주문 내역 요약을 추가했습니다.
## 운영 메모
- 21:31 · Order Friends Dev · `deposit_match_rows`, `cash_receipts` 테이블에 RLS를 적용해 운영용 입금 매칭 데이터와 현금영수증 데이터가 직접 노출되지 않도록 접근 정책을 강화했습니다.
- 21:50 · Order Friends Dev · 이 저장소에서 `npx supabase`를 바로 사용할 수 있도록 Supabase CLI를 dev dependency로 추가하고, 원격 적용에는 별도 `SUPABASE_ACCESS_TOKEN` 또는 `supabase login`이 필요하다는 점까지 확인했습니다.
- 22:30 · Order Friends Dev · `update_order_payment_status()` 함수에 `SET search_path = ''`를 적용해 Supabase 보안 경고의 mutable search_path 항목을 제거했습니다.
- 23:00 · Order Friends Dev · `brand_products`, `order_channels`, `procurement_*` 테이블에 브랜드/지점 멤버와 시스템 관리자 중심의 조회 정책을 추가하는 마이그레이션을 정리했습니다.
- 23:45 · Order Friends Dev · 원격 Supabase에도 위 정책을 적용해 security advisor 기준으로 남은 경고가 `Leaked password protection disabled` 한 건만 남도록 정리했습니다.
