# 2026-04-08 Patch Notes

## Improvements
- Improved the `/shop` landing page so the public brand list is rendered on the server first, reducing the blank and skeleton-only wait before customers can choose a brand.

## Operator Notes
- The page still falls back to the existing client-side `/public/brands` request if the server-side API base is unavailable.

## 버그 수정
- 11:31 · 김지훈 · server-render shop landing brands
- Fixed the deployed online shop card payment widget so it no longer re-initializes in a loop and trigger flickering payment errors.

- 13:53 · 김지훈 · stabilize shop card payment widget
- Made `/shop/...` checkout login redirects open the Kakao-first login flow instead of the generic email login screen.
