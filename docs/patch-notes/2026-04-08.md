# 2026-04-08 Patch Notes

## Improvements
- Improved the `/shop` landing page so the public brand list is rendered on the server first, reducing the blank and skeleton-only wait before customers can choose a brand.

## Operator Notes
- The page still falls back to the existing client-side `/public/brands` request if the server-side API base is unavailable.

## Bug Fixes
- 11:31 · 김지훈 · server-render shop landing brands
- Fixed the deployed online shop card payment widget so it no longer re-initializes in a loop and trigger flickering payment errors.
- 13:53 · 김지훈 · stabilize shop card payment widget
- Made `/shop/...` checkout login redirects open the Kakao-first login flow instead of the generic email login screen.
- 14:02 · 김지훈 · route shop checkout login to kakao
- Tightened the mobile `/shop/...` payment card spacing so the Toss card widget no longer crushes payment method labels on narrow screens.
- 14:40 · 김지훈 · polish mobile shop payment widget
- Fixed the remaining `/order/.../checkout` card widget flicker by stabilizing the Toss widget ready callback on both checkout routes.
- Widened the mobile `/shop/...` payment block to match the standard checkout layout more closely, reducing Toss widget text overflow on narrow screens.
- Moved the `/shop/...` desktop order box below the product list so the Toss payment widget can render at a full-width checkout size instead of a narrow sidebar width.
- Delayed the card-payment order completion KakaoTalk until the actual Toss payment approval succeeds, instead of sending it immediately when the order draft is created.
- Fixed card-payment confirmation to tolerate older production schemas that do not yet expose the delivery-address and fulfillment columns needed only for the delayed KakaoTalk payload.
- Fixed Toss card-payment confirmation so a KakaoTalk notification lookup/send failure no longer turns an already-approved payment into a visible `Failed to fetch order` error on the customer success screen.
- Split payment preparation from the delivery-only notification lookup so `/payments/prepare` no longer fails on older production schemas missing `orders.customer_address1`, `orders.customer_address2`, or `orders.fulfillment_type`.
- Matched the `/shop/...` top brand/profile header width to the main product/order cards so the desktop layout no longer looks wider than the content below it.

## 개선 사항
- 14:41 · 김지훈 · sync develop into main

## 버그 수정
- 15:22 · 김지훈 · stabilize order checkout payment widget

- 17:08 · 김지훈 · widen mobile shop payment layout

- 17:33 · 김지훈 · stack shop checkout layout on desktop

- 17:58 · 김지훈 · delay card order completion kakao until payment success

- 18:13 · 김지훈 · tolerate legacy order columns during card confirm

- 18:59 · 김지훈 · decouple card confirm from kakao notification failures

- 19:06 · 김지훈 · isolate payment prepare from delivery fields
