# 2026-04-12 패치노트

## 개선 사항
- 16:43 · Order Friends Dev · `/admin`은 시스템 관리자만, `(protected)` 영역은 실제 브랜드/지점 멤버만 통과하도록 프론트 접근 제어를 보강했습니다.
- 16:59 · Order Friends Dev · 공개 주문/결제 경로에 대한 E2E 계약 테스트를 추가해 비인증 주문 생성, 공개 결제 요청, 권한 경계를 자동 검증하도록 했습니다.
- 17:06 · Order Friends Dev · Toss webhook DTO에 검증 규칙을 추가해 전역 `ValidationPipe` 환경에서도 정상 webhook과 잘못된 요청을 구분하도록 맞췄습니다.
- 17:18 · Order Friends Dev · Toss Payments webhook의 전송 시각 검증을 추가해 유효 시간 범위를 벗어난 서명 요청을 거절하도록 보강했습니다.
- 17:14 · Order Friends Dev · 결제 상태 트리거의 최신 canonical 정의를 다시 적용하는 migration을 추가하고, CI에서 관련 migration drift를 검사하도록 했습니다.
- 17:14 · Order Friends Dev · backend Swagger 문서에서 타입을 생성하는 OpenAPI 파이프라인을 추가하고, CI에서 generated 타입 최신 여부를 검증하도록 연결했습니다.
- 17:32 · Order Friends Dev · `common.ts`와 매장 설정, 주문 체크아웃, 주문 추적 화면에 generated OpenAPI 타입을 실제로 연결해 화면 타입 중복을 줄이기 시작했습니다.
- 18:07 · Order Friends Dev · 레거시 `PublicModule`과 관련 DTO, 서비스, 전용 테스트를 제거해 공개 주문 경로를 `public-order` 축으로 정리했습니다.
- 23:40 · Order Friends Dev · 배송 주문과 관리자 주문 상세에서 공통으로 쓸 수 있도록 주문 상세/공개 주문 추적 응답에 읽기 전용 배송현황 필드를 추가할 기반을 마련했습니다.
- 23:58 · Order Friends Dev · 운영자가 배송 주문의 배송 상태, 배송사, 송장 번호를 수정할 수 있는 관리자용 배송현황 수정 API와 전이 검증을 추가했습니다.
- 23:59 · Order Friends Dev · 주문 추적 화면과 관리자 주문 상세에 배송현황 카드와 단계 표시를 추가하고, 관리자 화면에서 배송사·송장 번호·배송 상태를 함께 수정할 수 있게 했습니다.

## 운영 메모
- 배치 3은 공개 주문/결제 E2E, 결제 상태 트리거 canonical migration, CI migration/API contract 가드까지 포함하는 범위로 완료했습니다.
- 배치 4는 레거시 공개 주문 모듈 제거, OpenAPI 타입 생성 파이프라인, generated 타입의 프론트 1차 연동까지 반영했고, generated 타입의 화면 전면 치환은 후속 정리 대상으로 남겨뒀습니다.
