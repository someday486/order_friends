# 2026-04-13 패치노트

> 자동 추가 항목 형식: `- HH:MM · 작업자 · 변경 내용`

## 신규 기능
- 02:35 · Order Friends Dev · 결제/과금 이원화 Phase 2 범위로 `billing` 모듈, 토스 빌링키 발급, 월 자동 결제 배치, 관리자 빌링 API를 추가하고 OpenAPI 타입을 다시 생성했습니다.
- 02:36 · Order Friends Dev · 결제/과금 이원화 Phase 3 범위로 `settlement` 모듈, 월 정산 배치, 수수료 구간 CRUD, 브랜드 오너·관리자 정산 조회 API를 추가했습니다.

- 03:37 · Order Friends Dev · implement billing tiers and settlement workflows
## 개선 사항
- 00:18 · Order Friends Dev · 택배 주문이 `배송 완료`로 바뀌면 기존 알림 서비스를 재사용해 고객에게 카카오 알림톡 안내를 보낼 수 있도록 보강했습니다.
- 00:19 · Order Friends Dev · 관리자 배송현황 수정 경로에 대한 고객 주문 E2E를 추가해 권한 없는 요청과 잘못된 주문을 자동으로 검증하도록 보강했습니다.
- 00:26 · Order Friends Dev · 배송현황 노출과 수정 대상은 배달이 아니라 `택배(SHIPPING)` 주문에만 붙도록 범위를 바로잡았습니다.
- 00:41 · Order Friends Dev · `/shop` 온라인샵의 기본 주문 방식이 `배달(DELIVERY)`이 아니라 `택배(SHIPPING)`가 되도록 기본값과 주문 생성 흐름을 맞췄습니다.
- 02:37 · Order Friends Dev · 브랜드 생성 시 `billingTier`를 필수로 받아 PG/Non-PG를 처음부터 고정하고, PG 브랜드는 기본 수수료율과 카드 전용 결제 설정이 함께 저장되도록 보강했습니다.

## 버그 수정
- 00:52 · Order Friends Dev · 주문 추적 페이지가 브라우저 캐시를 첫 렌더 전에 읽으면서 서버 HTML과 달라지던 hydration 오류를 수정하고, 캐시 복원은 마운트 이후에만 일어나도록 정리했습니다.
- 00:58 · Order Friends Dev · 공개 주문 조회가 오래된 스키마의 누락 컬럼을 여러 번 만나면 fallback 재시도 한도에 먼저 걸려 404처럼 끝나던 문제를 수정해 최신 주문 상태 조회가 다시 정상 완료되도록 했습니다.
- 02:36 · Order Friends Dev · 월 정산 기간 계산이 UTC 직렬화 때문에 하루씩 밀리던 문제를 보정해 이전 달 시작일과 종료일이 서울 기준 달력과 정확히 맞도록 수정했습니다.

## 운영 메모
- 01:24 · Order Friends Dev · 결제/과금 이원화 Phase 0 범위로 브랜드 billing tier, 수수료율, 구독·빌링·정산 기초 테이블과 RLS 정책, 백엔드 enum 기반을 추가했습니다.
- 02:16 · Order Friends Dev · 결제/과금 이원화 Phase 1 범위로 PG/Non-PG billing tier 기반 결제 검증과 체크아웃 분기, 결제 성공 시 정산 line item 생성, 환불 시 마이너스 정산 line item 보정까지 반영했습니다.
- 02:59 · Order Friends Dev · 브랜드 OWNER뿐 아니라 활성 ADMIN도 billing·settlement 조회 API를 사용할 수 있도록 권한 범위를 보강했습니다.
- 03:00 · Order Friends Dev · `/business/payments`를 결제 운영 허브로 정리하고, `/business/billing`, `/business/settlement` 화면을 연결해 브랜드 티어별 운영 흐름을 한 번에 따라갈 수 있게 맞췄습니다.
- 03:11 · Order Friends Dev · /business/payments, /business/billing, /business/settlement 화면 문구와 billing/settlement API 문서를 UTF-8 한국어 기준으로 정리해 운영자가 결제·정산 흐름을 더 읽기 쉽게 다듬었습니다.
- 03:18 · Order Friends Dev · /business 레이아웃, 대시보드, 주문 히스토리, mock 데이터의 깨진 한글을 정리하고 결제·빌링·정산 메뉴 진입점을 같은 흐름으로 맞췄습니다.


## 버그 수정
- 07:41 · Order Friends Dev · `brands.billing_tier` 컬럼이 아직 없는 DB에서도 `/customer/brands`가 바로 실패하지 않도록 `shop_payment_methods` 기반 billing tier fallback을 추가했습니다.

## 운영 메모
- 08:10 · Order Friends Dev · 원격 DB에 실제로 빠져 있던 `order_idempotency`, `brand_analytics_indexes`, `brand_products` 백필 migration을 적용하고, 최근 billing Phase 0 스키마와 배송 tracking 컬럼도 함께 맞췄습니다.
- 08:12 · Order Friends Dev · 남아 있는 migration list 차이는 레거시 8자리/중복 버전 포맷에서 생기는 history drift로 좁혀졌고, 원격 전용 버전은 로컬 placeholder migration으로 정리했습니다.
- 10:24 · Order Friends Dev · 초창기 `20260206`, `20260212` 공유 버전 migration 파일을 고유 타임스탬프로 재정렬하고, legacy placeholder와 원격 history entry를 맞춰 migration drift를 더 줄였습니다.
## 추가 메모
- 12:40 · Order Friends Dev · 무통장 전용 브랜드의 빌링 화면에 무료 체험 배너, 현재 사용량, 플랜 변경 UI, 카드 미등록 시 등록 유도 화면을 보강했습니다.
- 12:41 · Order Friends Dev · 관리자 매장 화면에서 선택한 브랜드와 매장 상세에 결제 운영 방식(PG 이용 / 무통장 전용)을 함께 표시하도록 정리했습니다.
## 운영 메모
- 16:35 · Order Friends Dev · 원격 Supabase DB에 `subscription_plans.max_monthly_orders`, `brand_subscriptions.scheduled_plan_*` 컬럼을 실제 적용하고 migration history를 맞췄습니다.
- 16:36 · Order Friends Dev · 비어 있던 `subscription_plans` 기본 요금제(Starter/Growth/Pro)를 원격 DB에 채우고 이후 신규 환경용 seed migration도 추가했습니다.
