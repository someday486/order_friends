# 2026-04-14 패치노트

> 자동 추가 항목 형식: `- HH:MM · 작업자 · 변경 내용`

## 신규 기능
- 

## 개선 사항
- 00:02 · Order Friends Dev · 월 구독 빌링과 PG 정산 운영 화면을 `/customer` 영역으로 옮기고, 기존 `/business` 경로는 호환 리다이렉트로 정리했습니다.

## 버그 수정
- 00:08 · Order Friends Dev · 로컬 운영 서버가 `dist/main`을 찾지 못하던 문제를 정리하고 `start:prod` 실행 경로를 실제 빌드 산출물인 `dist/main.js`로 맞췄습니다.
- 00:12 · Order Friends Dev · Docker 런타임도 같은 엔트리 파일을 보도록 `dist/main.js` 기준으로 정리해 로컬 실행 경로와 배포 실행 경로를 일치시켰습니다.
- 00:26 · Order Friends Dev · 월 구독이 아직 없는 `NON_PG` 브랜드는 `/shop` 공개 온라인샵에 진입하거나 주문을 만들 수 없도록 구독 상태 기반 접근 제어를 추가했습니다.

- 00:17 · Order Friends Dev · restrict non-pg storefront access
## 운영 메모
- 지연이나 배포 관련 메모가 있을 때만 작성합니다.

